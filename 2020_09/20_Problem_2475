#include <iostream>

using namespace std;

template <class T>
T inline pow(T a)
{
	return a*a;
}

int main(void)
{
	int sum = 0;
	int t;
	for(int i = 0 ; i < 5 ; i ++)
	{
		cin >> t;
		sum = sum + pow(t);
	}
	
	cout << sum%10;
	
}
