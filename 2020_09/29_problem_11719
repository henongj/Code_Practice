#include <iostream>
#include <string>
#include <vector>
using namespace std;

typedef vector<string> vecstr;

int main() {
	string str;
	vecstr v;
	for( ; !cin.eof(); )
	{
		getline(cin,str);
		v.push_back(str);
	}
	
	for(int i = 0 ; i<v.size() ; i++)
		cout << v[i] << "\n";

	return 0;
}
