#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main(void)
{
	string number;
	cin >> number;
	
	sort(number.begin(),number.end());
	
	for(int i = number.length()-1 ; i >= 0 ; i--)
		cout << number[i];

	return 0;
}
