#include<iostream>

using namespace std;

bool is_leapYear(int year)
{
    return (year%4 == 0 && year%100 !=0) || (year%400 == 0) ? true : false;
}
int main(void)
{
    int year;
    cin >> year;
    is_leapYear(year)? cout << "1" : cout << "0";
    return 0;
}
