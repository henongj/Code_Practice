#include<iostream>
#include<string>
#include<vector>
#include<queue>
using namespace std;

inline bool isInside(const int& n)
{
	return 0 <= n && n <= 500000 ? true : false;
}

void setQueue(bool checkButton[] , queue<int>& q)
{// 1이 사용가능이면 1 11 111 1111 이런 숫자들의 횟수입력하는 함수
	int pushCount = 1;
	int idx = 0;
	while(isInside(idx))
	{
		
	}
}

int main(void)
{
	int buttonCounter[500001] = {};
	
	int target;
	int brokenButtonSize;
	bool checkButton[11]; // 사용가능한 숫자
	
	cin >> target;
	cin >> brokenButtonSize;
	
	for(int i = 0 ; i < 11 ; i++)
		checkButton[i] = true;
	for(int i = 0 ; i < brokenButtonSize ; i++)
	{
		int t;
		cin >> t;
		checkButton[i] = false;
	}
	
	queue<int> q;
	
	return 0;
}

/*




*/

